server:
  port: 8006
spring:
  application:
    name: demo-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/limaoecho/config.git
          username: limaoecho
          password: limao111111
          search-paths: user